# 粤见 CantoScene｜前端实现说明

> 文档状态：MVP 前后端接入版
> 依赖文档：《06-视觉设计规范》《07-组件与交互规范》《08-响应式与高保真页面设计》《09-页面文案与示范内容》  
> 前置状态：视觉方向与桌面端页面设计已经项目负责人确认，本文据此锁定技术细节  
> 本文用途：设计向开发的交接，作为前端项目搭建的直接依据

## 1. 技术选型（默认方案，可讨论调整）

| 项 | 选择 | 理由 |
|---|---|---|
| 框架 | Vite + React + TypeScript | 组件边界清晰，类型约束适合结构化内容 |
| 路由 | React Router | 页面少，无服务端渲染需求 |
| 样式 | CSS 自定义属性 + 模块 CSS | 设计 token 已在 06 定义为变量，直接映射 |
| 状态 | React Context + useState/useReducer | 状态范围小（见 §4），不引入大型状态库 |
| 简繁转换 | opencc-js（hk2s／s2hk） | 客户端即时转换，例外表本地维护 |
| 图标 | lucide-react | 与 06 规范的圆头线性风格一致 |
| 视频 | 原生 HTML5 video 自绘控制条 | 控制条样式已在原型定义 |

不自建构建之外的复杂基础设施；管理后台不在本期前端范围。

## 2. 页面路由

| 路径 | 页面 | 对应原型 |
|---|---|---|
| / | 首页（游客／登录两态） | home.html |
| /courses | 基础课程首页 | courses.html |
| /courses/:unitId | 单元介绍（可并入课程首页锚点） | — |
| /lessons/:lessonId | 课节学习页 | lesson.html |
| /jyutping | 粤拼发音速查 | 待出（08 §10.1） |
| /words | 高频粤语字词 | 待出（08 §10.2） |
| /videos | 影视视频列表 | videos.html |
| /watch/:videoId | 视频学习页 | video-study.html |
| /favorites | 收藏页 | favorites.html |
| /my | 我的学习 | my.html |
| /login | 登录／注册（同时支持对话框形态） | 07 §11 |

规则：/watch/:videoId?t=秒 支持时间定位（收藏回跳用）；未登录访问 /favorites、/my 时展示引导页而非强制跳转。

## 3. 组件边界

```text
src/
  components/
    chrome/        SiteNav, SiteFooter, LangToggle, LoginDialog
    ui/            Button, Chip, Stamp, RouteProgress, PlayButton,
                   Tape, EmptyState, ErrorState, Skeleton
    course/        ShopCard, MenuSidebar, DishCard, JyutTile, DialogueLine
    video/         VideoCard, FilmPlayer, SubtitleList, SubtitleRow,
                   SubtitlePostcard, StickyNote
    favorite/      TicketCard, WordCard, MiniTicket, StatusMenu
    my/            ItineraryCard, StatRings, UnitProgressRow, RecentCard
  pages/           与 §2 路由一一对应
  data/            Mock 数据与类型（见 §5）
  lib/             opencc 封装、音频单例播放器、进度存取
  styles/          tokens.css（06 规范变量）、base.css
```

边界原则：

- 视觉件（Tape、Stamp、Chip）为纯展示组件，不含业务逻辑；
- SubtitleList 拥有"自动跟随／主动学习"状态机，是交互最重的组件，单独成文件；
- FilmPlayer 只暴露 currentTime、seek、playSegment(start,end) 等命令式接口，字幕同步逻辑在 SubtitleList 侧；
- 音频全局单例（lib/audio.ts），保证同时只播一条（07 §8）。

## 4. 状态管理范围

| 状态 | 位置 | 持久化 |
|---|---|---|
| 当前用户／登录态 | AppContext + API Client | 内存 access token + HttpOnly refresh cookie |
| 简繁偏好 | AppContext | 游客存 localStorage，登录后同步数据库 |
| 课程／视频进度 | AppContext + API Client | 登录后写入服务端 |
| 收藏列表／生词本 | AppContext + API Client | 按用户写入服务端 |
| 播放器瞬时状态 | 视频学习页本地 state | 不持久化 |
| 音频播放状态 | lib/audio 单例 | 不持久化 |

## 5. Mock 数据结构（TypeScript 类型即契约）

```ts
interface CourseUnit {
  id: string;            // "unit-jyutping"
  order: number;         // 1
  shopName: string;      // "粤拼文具店"（视觉配置）
  awning: "sky" | "peach" | "butter";
  title: string;         // "粤拼与声调入门"
  intro: string;
  lessonCount: number;
  minutes: number;
  lessons: Lesson[];
}

interface Lesson {
  id: string;
  unitId: string;
  order: number;
  title: string;         // "第 2 课：说出你想要什么"
  goal: string;
  stages: LessonStage[]; // intro | words | patterns | dialogue | quiz | summary
  recommendVideoId?: string;
}

interface WordEntry {
  id: string;
  code?: string;         // "A1"（课节内编号）
  yue: string;           // "冻"
  jyutping: string;      // "dung3"
  meaning: string;       // "冷的；加冰的"
  example: { yue: string; jyutping: string; zh: string };
  audioWord: string;     // 音频资源 key，见 §7
  audioExample: string;
  tip?: string;
  relatedUnitId?: string;
}

interface DialogueLine {
  speaker: string;       // "店员"
  yue: string; jyutping: string; zh: string;
  audio: string;
}

interface VideoClip {
  id: string;
  title: string;         // "茶餐厅点餐：一杯冻柠茶"
  listTitle: string;     // "茶餐厅点饮品"
  level: "入门" | "基础" | "进阶";
  durationSec: number;   // 92
  tags: string[];        // ["点餐","冷热甜度"]
  intro: string;
  focus: string[];       // 本段重点
  relatedUnitId?: string;
  placeholderGlyph: string; // "茶"（正式封面前）
  videoUrl?: string;     // 占位期为空 → 播放器显示占位态
  subtitles: SubtitleLine[];
}

interface SubtitleLine {
  id: string;
  start: number;         // 秒，5.0
  end: number;           // 8.0
  speaker?: string;
  yue: string;
  jyutping?: string;     // 缺省时前端显示"解释待完善"
  zh?: string;
}

interface Favorite {
  id: string;
  kind: "line" | "word";
  refId: string;         // SubtitleLine.id 或 WordEntry.id
  videoId?: string;      // kind=line 时必有
  start?: number;        // 原声定位
  status: "刚收藏" | "学习中" | "已掌握";
  createdAt: number;
}

interface Progress {
  lessons: Record<string, number>;       // lessonId → 0..1
  videos: Record<string, number>;        // videoId → 看到第几秒
  studyDays: number;
}
```

Mock 数据初值即《09-页面文案与示范内容》全文，放在 src/data/，按类型分文件。

## 6. 视频与字幕的输入方式

- 占位期：VideoClip.videoUrl 为空，播放器显示占位画面（衬线单字 + 占位说明），字幕、展开、收藏、进度全部照常可用（用 virtualTime 模拟播放进度，便于验证同步逻辑）；
- 正式素材：项目负责人提供视频文件后，放入资源目录并在数据中填 videoUrl，页面零改动切换；
- 字幕数据只通过 VideoClip.subtitles 进入前端，起止时间以秒为单位（number），UI 层格式化为 mm:ss；
- 不允许前端抓取第三方影视资源（产品约束 17）。

## 7. 音频资源组织

```text
public/audio/
  words/      词条发音（word-{id}.mp3）
  examples/   例句发音（ex-{id}.mp3）
  dialogue/   情景对话单句与整段（dlg-{lessonId}-{n}.mp3）
  jyutping/   速查发音（jp-{initial|final|tone}-{key}.mp3）
```

- 全部音频离线预生成（Fish Audio 指定女声，ID：be60c03139d94dc7b0c26d83f66550db），前端只播静态文件，不调用 TTS 接口（产品约束 16）；
- 语音生成输入用香港繁体原稿，界面默认简体不受影响；
- 文件命名与数据中的 audio key 一致，构建期校验缺失文件。

## 8. 简繁切换范围

- 用 opencc-js：简体界面 → 香港繁体为 s2hk，反向 hk2s；
- 转换对象：界面文案 + 内容文本的字形；粤语原文、粤拼、音频不因切换改变（产品约束 4）；
- 例外表 src/data/opencc-exceptions.json，随内容录入维护（粤语特有字、一简对多繁）；
- 切换即时生效、保留页面与播放状态；偏好存储见 §4。

## 9. 登录与游客体验

- MVP 已接入邮箱密码注册和登录，密码只保存 scrypt 哈希；
- 触发点：收藏、保存进度、修改状态、访问 /favorites 与 /my；
- 登录后回到触发位置并完成原操作（07 §11）；
- 访问令牌仅存内存，刷新页面后通过 HttpOnly cookie 换取新令牌；旧版本地收藏会在首次真实登录后尝试迁移。

## 10. API 接入

> 真实后端的数据结构、接口路径、鉴权和错误契约以《13-数据库结构设计》和《14-前后端接口设计》为准。

MVP 数据层位于 `src/api/client.ts`，当前已接入：

```ts
register()/login()/restoreSession()/logout()
getVideo()/getSubtitles()
getFavorites()/addFavorite()/deleteFavorite()
getWordbook()/addWord()/deleteWord()
getSummary()/saveVideoProgress()
```

默认 API 地址为 `http://127.0.0.1:3000/api/v1`，部署时可用 `VITE_API_BASE_URL` 覆盖。

## 11. 资源目录

```text
public/
  img/        封面插画、占位封面素材（来自 design/assets，构建时拷入）
  audio/      见 §7
  video/      正式片段（项目负责人后续提供）
src/styles/tokens.css   06 规范全部色值／字体／圆角／阴影变量
```

字体：系统字体栈（06 §4），不加载网络字体；后续若引入 Noto 系 webfont，授权说明放 design/fonts/。

## 12. 环境变量与密钥

- 前端仓库不放任何密钥；Fish Audio API 密钥只出现在内容生产环境（本地 .env，不提交），前端运行时完全不需要它；
- .env.example 只保留占位键名（如 VITE_API_BASE）；
- .gitignore 默认排除 .env*。

## 13. 浏览器与设备支持

- 桌面 Chrome / Edge / Safari / Firefox 最近两个大版本；
- 视口 1024px 起保证完整功能，1280–1440 为设计基准；
- 移动端浏览器本期不保证布局（已知限制，移动端版本启动后单独验收）；
- 容器查询单位（cqw）与 backdrop-filter 需特性检测降级（旧浏览器退化为固定字号与不透明底色）。

## 14. 从原型到实现的迁移要点

- design/mockup/css/site.css 的类名与组件一一对应，可作为 CSS 起点但不直接搬运（改模块化）；
- 原型的装饰性细节（纸胶带、印章旋转、票根打孔）均为纯 CSS，无图片依赖；
- 首页首屏插图为唯一大体积位图，构建时压缩并输出 2x 分辨率版本；
- 交互验收标准全部来自《11-前端验收清单》。
