# 粤见 CantoScene｜前后端接口设计

> 文档状态：MVP v1 草案  
> 适用范围：用户、课程、视频字幕、收藏、生词本、学习进度及内容处理任务  
> 关联文档：[02-MVP-PRD.md](./02-MVP-PRD.md)、[10-前端实现说明.md](./10-前端实现说明.md)、[13-数据库结构设计.md](./13-数据库结构设计.md)

## 1. 设计目标

本接口用于把当前前端中的模拟数据和 `localStorage` 状态替换为真实后端能力。接口保持技术栈中立，后续无论采用 Node.js、Python，或接入托管数据库与认证服务，前端数据契约都不需要随之重写。

MVP 接口覆盖：

- 注册、登录与用户偏好；
- 课程、课节及课节内容；
- 视频、分时字幕与字幕学习信息；
- 字幕句和课程内容收藏；
- 生词本；
- 课程、课节和视频学习进度；
- 字幕导入、DeepSeek 辅助处理与内容审核状态。

## 2. 接口总则

### 2.1 基础约定

| 项目 | 约定 |
|---|---|
| 协议 | HTTPS |
| 风格 | REST JSON API |
| 基础路径 | `/api/v1` |
| 字符编码 | UTF-8 |
| 数据库 ID | UUID 字符串 |
| 业务时间 | 毫秒整数，例如 `start_ms: 33000` |
| 创建/更新时间 | ISO 8601 UTC，例如 `2026-08-08T08:00:00Z` |
| 分页 | 游标分页 `cursor + limit` |
| 默认语言 | 简体中文；用户可切换香港繁体 |
| 字段命名 | JSON 使用 `snake_case` |

服务端应为每次请求生成 `request_id`，方便前后端联调和问题定位。

### 2.2 身份认证

- 受保护接口使用 `Authorization: Bearer <access_token>`；
- 刷新令牌优先放入 `HttpOnly`、`Secure`、`SameSite` Cookie；
- 若采用第三方认证服务，业务接口仍通过后端解析出的 `user_id` 鉴权；
- 前端不得保存密码、DeepSeek Key、Fish Audio Key 或数据库管理密钥；
- 游客可浏览已发布内容，收藏、生词本和学习进度要求登录。

### 2.3 成功响应

单项数据：

```json
{
  "data": {
    "id": "4ab16f8e-a4cb-4f0c-867a-a5c220df2d11"
  },
  "meta": {
    "request_id": "req_01J4..."
  }
}
```

列表数据：

```json
{
  "data": [],
  "meta": {
    "next_cursor": null,
    "has_more": false,
    "request_id": "req_01J4..."
  }
}
```

### 2.4 错误响应

```json
{
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "提交的数据不符合要求",
    "details": [
      {
        "field": "current_ms",
        "reason": "must_be_non_negative"
      }
    ],
    "request_id": "req_01J4..."
  }
}
```

稳定错误码：

| 错误码 | HTTP | 使用场景 |
|---|---:|---|
| `AUTH_REQUIRED` | 401 | 未登录或令牌失效 |
| `FORBIDDEN` | 403 | 无权访问或编辑 |
| `NOT_FOUND` | 404 | 资源不存在 |
| `CONTENT_UNPUBLISHED` | 404 | 普通用户请求未发布内容 |
| `VALIDATION_ERROR` | 422 | 字段格式、范围或业务校验失败 |
| `CONFLICT` | 409 | 唯一约束、版本冲突或重复操作 |
| `VIDEO_UNAVAILABLE` | 503 | 视频源暂时不可用 |
| `SUBTITLE_UNAVAILABLE` | 503 | 字幕尚未导入或处理失败 |
| `RATE_LIMITED` | 429 | 请求频率过高 |
| `AI_PROVIDER_ERROR` | 502 | DeepSeek 等服务调用失败，仅管理端可见 |
| `INTERNAL_ERROR` | 500 | 未预期的服务端错误 |

客户端只依赖 `code` 做程序判断，`message` 用于展示或辅助排查。

## 3. 核心数据对象

### 3.1 用户 `User`

```json
{
  "id": "5d29ca2f-c9ac-4908-b79a-819b8e1466f8",
  "display_name": "粤语初学者",
  "avatar_url": null,
  "role": "learner",
  "script_preference": "simplified",
  "created_at": "2026-08-08T08:00:00Z"
}
```

公开响应不返回邮箱、认证服务标识或内部管理字段。

### 3.2 课程摘要 `CourseSummary`

```json
{
  "id": "64b11877-baa6-4dc7-99f4-4a420c88c251",
  "slug": "cha-chaan-teng-starter",
  "title_simplified": "茶餐厅点餐",
  "title_traditional": "茶餐廳點餐",
  "summary_simplified": "从真实点餐场景开始学习。",
  "summary_traditional": "從真實點餐場景開始學習。",
  "cover_url": "/images/courses/cha-chaan-teng.webp",
  "difficulty": "starter",
  "lesson_count": 5,
  "estimated_minutes": 35,
  "progress": null
}
```

登录用户请求时，`progress` 可返回派生值：

```json
{
  "completed_lessons": 1,
  "total_lessons": 5,
  "percent": 20
}
```

课程总进度不单独写入数据库，由课节进度聚合生成。

### 3.3 课节 `LessonDetail`

```json
{
  "id": "c878cc79-f27b-4ef2-978f-21153034458d",
  "course_id": "64b11877-baa6-4dc7-99f4-4a420c88c251",
  "slug": "ordering-drinks",
  "title_simplified": "点饮品",
  "title_traditional": "點飲品",
  "summary_simplified": "学习茶餐厅常用饮品表达。",
  "summary_traditional": "學習茶餐廳常用飲品表達。",
  "position": 1,
  "estimated_minutes": 8,
  "items": [],
  "progress": null,
  "related_videos": []
}
```

课节内容项统一包含：

```json
{
  "id": "1dc31d25-26de-478c-a202-6629a49f6641",
  "type": "phrase",
  "position": 1,
  "content": {
    "text_simplified": "我要一杯冻柠茶。",
    "text_traditional": "我要一杯凍檸茶。",
    "jyutping": "ngo5 jiu3 jat1 bui1 dung3 ning4 caa4",
    "mandarin_simplified": "我要一杯冰柠檬茶。",
    "mandarin_traditional": "我要一杯冰檸檬茶。",
    "audio_url": "/audio/lessons/dung-ning-caa.mp3"
  }
}
```

MVP 允许的 `type`：`intro`、`character`、`phrase`、`dialogue`、`tip`。不同类型由后端 JSON Schema 校验，避免任意结构直接进入数据库。

### 3.4 视频 `VideoDetail`

```json
{
  "id": "3e11b180-70bc-4095-8fa5-c1450be5110f",
  "slug": "cha-chaan-teng",
  "title_simplified": "粤语对白练习 01",
  "title_traditional": "粵語對白練習 01",
  "summary_simplified": "跟随真实片段听粤语对白。",
  "summary_traditional": "跟隨真實片段聽粵語對白。",
  "video_url": "/videos/01.mp4",
  "poster_url": "/images/videos/01.webp",
  "duration_ms": 1099000,
  "difficulty": "starter",
  "tags": ["职场", "律师楼", "日常对白"],
  "subtitle_count": 350,
  "progress": null
}
```

### 3.5 字幕句 `SubtitleLine`

```json
{
  "id": "71343e6c-a2ac-4dc2-a439-fd7f7a2c2c9d",
  "video_id": "3e11b180-70bc-4095-8fa5-c1450be5110f",
  "position": 12,
  "start_ms": 33000,
  "end_ms": 37000,
  "text_simplified": "在他身边都不知道，多养靓身材好的异性。",
  "text_traditional": "在他身邊都不知道，多養靚身材好的異性。",
  "jyutping": null,
  "mandarin_simplified": null,
  "mandarin_traditional": null,
  "audio_url": null,
  "is_favorited": false
}
```

说明：

- `audio_url` 为空时，前端用原视频的 `start_ms` 和 `end_ms` 重播该句；
- 未校对或未生成的学习字段返回 `null`，前端展示“待校对后生成”，不得伪造内容；
- 普通用户接口不返回 `review_status`、AI 原始结果和编辑备注；
- 字幕正文即使存在，也只有在视频与字幕均发布后才对游客可见。

### 3.6 收藏 `Favorite`

```json
{
  "id": "5015c37f-989d-49b8-8b3a-16c0e9d9c799",
  "kind": "subtitle_line",
  "status": "learning",
  "note": null,
  "created_at": "2026-08-08T08:00:00Z",
  "subtitle_line": {
    "id": "71343e6c-a2ac-4dc2-a439-fd7f7a2c2c9d",
    "video_id": "3e11b180-70bc-4095-8fa5-c1450be5110f",
    "video_slug": "cha-chaan-teng",
    "start_ms": 33000,
    "text_simplified": "在他身边都不知道，多养靓身材好的异性。",
    "text_traditional": "在他身邊都不知道，多養靚身材好的異性。",
    "jyutping": null
  },
  "lesson_item": null
}
```

`kind` 为 `subtitle_line` 或 `lesson_item`。服务端只返回与 `kind` 对应的嵌套对象。

### 3.7 生词本条目 `WordbookItem`

```json
{
  "id": "b8c61676-b1aa-4c46-8202-0615e0339f21",
  "term_simplified": "冻柠茶",
  "term_traditional": "凍檸茶",
  "jyutping": "dung3 ning4 caa4",
  "mandarin_simplified": "冰柠檬茶",
  "mandarin_traditional": "冰檸檬茶",
  "example_simplified": "唔该，一杯冻柠茶。",
  "example_traditional": "唔該，一杯凍檸茶。",
  "audio_url": null,
  "status": "learning",
  "source_type": "subtitle_line",
  "source_id": "71343e6c-a2ac-4dc2-a439-fd7f7a2c2c9d",
  "note": null,
  "created_at": "2026-08-08T08:00:00Z",
  "updated_at": "2026-08-08T08:00:00Z"
}
```

收藏和生词本是两种行为：收藏保留原内容引用；生词本保存用户准备专门复习的词或表达，两者不自动互相创建。

### 3.8 学习进度

课节进度：

```json
{
  "lesson_id": "c878cc79-f27b-4ef2-978f-21153034458d",
  "status": "in_progress",
  "last_item_position": 3,
  "completed_at": null,
  "updated_at": "2026-08-08T08:00:00Z"
}
```

视频进度：

```json
{
  "video_id": "3e11b180-70bc-4095-8fa5-c1450be5110f",
  "status": "in_progress",
  "current_ms": 38000,
  "duration_ms": 1099000,
  "percent": 3,
  "completed_at": null,
  "updated_at": "2026-08-08T08:00:00Z"
}
```

`percent` 为响应派生字段，不由客户端提交。视频进度达到服务端规定阈值时可自动标记为 `completed`，也允许用户主动标记完成。

## 4. 用户与认证接口

| 方法 | 路径 | 权限 | 用途 |
|---|---|---|---|
| `POST` | `/auth/register` | 游客 | 注册账号 |
| `POST` | `/auth/login` | 游客 | 登录并建立会话 |
| `POST` | `/auth/refresh` | 刷新令牌 | 刷新访问令牌 |
| `POST` | `/auth/logout` | 登录 | 注销当前会话 |
| `GET` | `/me` | 登录 | 获取当前用户资料和偏好 |
| `PATCH` | `/me/preferences` | 登录 | 修改简繁体和显示偏好 |

注册请求示例：

```json
{
  "email": "learner@example.com",
  "password": "用户输入的密码",
  "display_name": "粤语初学者"
}
```

若使用托管认证服务，密码请求可直接由认证服务处理；业务后端仍只保存对应用户资料。

偏好更新：

```json
{
  "script_preference": "traditional"
}
```

## 5. 课程接口

| 方法 | 路径 | 权限 | 用途 |
|---|---|---|---|
| `GET` | `/courses` | 公开 | 已发布课程列表 |
| `GET` | `/courses/{course_slug}` | 公开 | 课程详情与课节目录 |
| `GET` | `/lessons/{lesson_id}` | 公开 | 课节、内容项和关联视频 |

`GET /courses` 查询参数：

| 参数 | 类型 | 说明 |
|---|---|---|
| `difficulty` | string | `starter`、`beginner`、`intermediate`、`advanced` |
| `cursor` | string | 下一页游标 |
| `limit` | integer | 默认 20，最大 50 |

课程详情响应中的课节目录：

```json
{
  "id": "c878cc79-f27b-4ef2-978f-21153034458d",
  "slug": "ordering-drinks",
  "title_simplified": "点饮品",
  "title_traditional": "點飲品",
  "position": 1,
  "estimated_minutes": 8,
  "is_available": true,
  "progress": null
}
```

所有公开课程接口只返回 `published` 内容。登录后同一接口附带当前用户进度，未登录时为 `null`。

## 6. 视频与字幕接口

| 方法 | 路径 | 权限 | 用途 |
|---|---|---|---|
| `GET` | `/videos` | 公开 | 已发布视频列表 |
| `GET` | `/videos/{video_slug}` | 公开 | 视频详情 |
| `GET` | `/videos/{video_id}/subtitles` | 公开 | 获取分时字幕 |

字幕查询参数：

| 参数 | 类型 | 说明 |
|---|---|---|
| `cursor` | string | 分页游标 |
| `limit` | integer | 默认 200，最大 500 |
| `from_ms` | integer | 只返回结束时间晚于此值的字幕 |
| `to_ms` | integer | 只返回开始时间早于此值的字幕 |

对于 MVP 的短视频，前端可以一次取回全部字幕。接口仍保留时间窗口和分页能力，避免后续长视频需要重做契约。

字幕列表必须按 `position ASC` 返回。服务端校验同一视频的 `position` 唯一、`start_ms < end_ms`，并在导入时报告明显重叠或越界的字幕。

## 7. 收藏接口

| 方法 | 路径 | 权限 | 用途 |
|---|---|---|---|
| `GET` | `/me/favorites` | 登录 | 获取收藏列表 |
| `PUT` | `/me/favorites/subtitles/{subtitle_line_id}` | 登录 | 幂等收藏字幕句 |
| `PUT` | `/me/favorites/lesson-items/{lesson_item_id}` | 登录 | 幂等收藏课程内容 |
| `PATCH` | `/me/favorites/{favorite_id}` | 登录 | 修改学习状态或笔记 |
| `DELETE` | `/me/favorites/{favorite_id}` | 登录 | 取消收藏 |

列表查询参数：

```text
kind=subtitle_line|lesson_item
status=saved|learning|mastered|archived
cursor=<opaque_cursor>
limit=20
```

`PUT` 成功时无论此前是否已收藏，都返回当前唯一收藏记录，因此前端重复点击或网络重试不会产生重复数据。

修改请求：

```json
{
  "status": "mastered",
  "note": "在茶餐厅场景复习"
}
```

取消收藏后，不删除由该收藏手动加入的生词本条目。

## 8. 生词本接口

| 方法 | 路径 | 权限 | 用途 |
|---|---|---|---|
| `GET` | `/me/wordbook` | 登录 | 获取生词本 |
| `POST` | `/me/wordbook` | 登录 | 新增词语或表达 |
| `PATCH` | `/me/wordbook/{wordbook_item_id}` | 登录 | 修改内容、状态或笔记 |
| `DELETE` | `/me/wordbook/{wordbook_item_id}` | 登录 | 删除生词本条目 |

新增请求：

```json
{
  "term_simplified": "冻柠茶",
  "term_traditional": "凍檸茶",
  "jyutping": "dung3 ning4 caa4",
  "mandarin_simplified": "冰柠檬茶",
  "mandarin_traditional": "冰檸檬茶",
  "example_simplified": "唔该，一杯冻柠茶。",
  "example_traditional": "唔該，一杯凍檸茶。",
  "source_type": "subtitle_line",
  "source_id": "71343e6c-a2ac-4dc2-a439-fd7f7a2c2c9d"
}
```

前端从字幕加入生词本时，可以预填字幕及其学习信息，再让用户选取或修改要保存的词。后端不能假定整句字幕就是一个“生词”。

## 9. 学习进度接口

| 方法 | 路径 | 权限 | 用途 |
|---|---|---|---|
| `PUT` | `/me/lesson-progress/{lesson_id}` | 登录 | 幂等更新课节进度 |
| `PUT` | `/me/video-progress/{video_id}` | 登录 | 幂等更新视频播放进度 |
| `GET` | `/me/learning-summary` | 登录 | 首页或“我的学习”汇总 |
| `POST` | `/me/import-local-state` | 登录 | 首次登录时迁移本地 MVP 状态 |

课节进度请求：

```json
{
  "status": "in_progress",
  "last_item_position": 3
}
```

视频进度请求：

```json
{
  "current_ms": 38000,
  "status": "in_progress",
  "client_updated_at": "2026-08-08T08:00:00Z"
}
```

视频播放不应每一帧写数据库。建议前端在以下时机更新：

- 每 10 至 15 秒且播放位置发生变化；
- 暂停、跳转或离开页面；
- 播放完成；
- 浏览器进入后台前。

服务端处理乱序请求时，以 `client_updated_at` 和当前保存状态判断是否接受更新；已完成状态不能因旧请求退回 `in_progress`。

学习汇总响应：

```json
{
  "data": {
    "courses_started": 1,
    "lessons_completed": 2,
    "videos_started": 1,
    "videos_completed": 0,
    "favorites_learning": 4,
    "wordbook_learning": 7,
    "continue_learning": {
      "type": "video",
      "video_slug": "cha-chaan-teng",
      "current_ms": 38000
    }
  },
  "meta": {
    "request_id": "req_01J4..."
  }
}
```

### 9.1 本地状态迁移

当前 MVP 使用 `localStorage` 保存收藏、简繁偏好和学习进度。用户第一次登录后，前端可以调用一次迁移接口：

```json
{
  "schema_version": 1,
  "idempotency_key": "device-generated-uuid",
  "script_preference": "simplified",
  "favorites": [],
  "lesson_progress": [],
  "video_progress": []
}
```

规则：

- `idempotency_key` 防止刷新页面导致重复导入；
- 只接受能映射到现有公开资源的 ID；
- 收藏按唯一约束合并；
- 进度取较新或更完整的记录；
- 导入成功后，前端再清理本地业务状态；
- 生词本当前前端尚未形成可靠本地结构，不在首轮自动迁移中猜测生成。

## 10. 内容管理与 AI 处理接口

这组接口只供 `editor` 或 `admin` 使用，不由普通学习页面直接调用。

### 10.1 内容管理

| 方法 | 路径 | 权限 | 用途 |
|---|---|---|---|
| `POST` | `/admin/courses` | 编辑 | 创建课程草稿 |
| `PATCH` | `/admin/courses/{course_id}` | 编辑 | 修改课程 |
| `POST` | `/admin/courses/{course_id}/lessons` | 编辑 | 创建课节 |
| `PATCH` | `/admin/lessons/{lesson_id}` | 编辑 | 修改课节 |
| `PUT` | `/admin/lessons/{lesson_id}/items` | 编辑 | 批量校验并保存课节内容项 |
| `POST` | `/admin/videos` | 编辑 | 创建视频记录 |
| `PATCH` | `/admin/videos/{video_id}` | 编辑 | 修改视频资料 |
| `POST` | `/admin/videos/{video_id}/subtitle-imports` | 编辑 | 导入 SRT、VTT 或结构化字幕 |
| `PATCH` | `/admin/subtitle-lines/{subtitle_line_id}` | 编辑 | 校对单句字幕与学习字段 |
| `POST` | `/admin/videos/{video_id}/publish` | 编辑 | 校验并发布视频及字幕 |

视频文件先上传到对象存储或媒体服务，再由 `/admin/videos` 保存媒体 URL、存储键和元数据。不要把大型视频通过业务服务器 JSON 接口传输或放进数据库。

### 10.2 DeepSeek 辅助处理

| 方法 | 路径 | 权限 | 用途 |
|---|---|---|---|
| `POST` | `/admin/videos/{video_id}/ai-enrichment-jobs` | 编辑 | 创建字幕学习信息生成任务 |
| `POST` | `/admin/lessons/{lesson_id}/ai-enrichment-jobs` | 编辑 | 辅助生成课程内容草稿 |
| `POST` | `/admin/lessons/{lesson_id}/tts-jobs` | 编辑 | 创建 Fish Audio 静态音频任务 |
| `GET` | `/admin/content-jobs/{job_id}` | 编辑 | 查询任务状态与错误摘要 |

创建视频字幕辅助任务：

```json
{
  "fields": [
    "text_traditional",
    "jyutping",
    "mandarin_simplified",
    "mandarin_traditional"
  ],
  "line_ids": [],
  "overwrite_mode": "empty_or_ai_draft"
}
```

`overwrite_mode` 的 MVP 可选值：

- `empty_only`：只填空字段；
- `empty_or_ai_draft`：允许更新空字段和未人工确认的 AI 草稿；
- 不提供覆盖 `reviewed` 内容的模式。

任务响应：

```json
{
  "data": {
    "id": "f1c9bdae-a34f-4e43-8989-f6d6dde92a96",
    "type": "deepseek_subtitle_enrichment",
    "status": "queued",
    "total_items": 350,
    "processed_items": 0,
    "failed_items": 0,
    "created_at": "2026-08-08T08:00:00Z"
  },
  "meta": {
    "request_id": "req_01J4..."
  }
}
```

### 10.3 AI 内容处理流程

1. 编辑者导入带时间轴的原始字幕；
2. 服务端按句数和字符数分批，仅发送任务需要的文本；
3. 服务端调用 DeepSeek，要求返回严格 JSON；
4. 后端对字段数量、行 ID、粤拼格式和长度做结构校验；
5. 结果写入空字段或 `ai_draft` 字段，不覆盖人工审核内容；
6. 编辑者在管理端逐批检查并标记 `reviewed`；
7. 只有通过发布校验的内容进入公开接口。

DeepSeek 调用失败不能影响视频播放或已发布字幕。任务保留失败项目和可重试状态，公开页面继续使用已有内容。

DeepSeek 与 Fish Audio 的密钥只存在于服务器环境变量或密钥管理服务中，不写入数据库业务表、不返回前端、不记录完整请求密钥。

## 11. 前端现有模块与接口映射

| 当前前端能力 | 正式接口 | 迁移说明 |
|---|---|---|
| `getUnits()` | `GET /courses` | 单元概念统一为课程列表 |
| `getLesson(id)` | `GET /lessons/{lesson_id}` | 返回课节内容项和进度 |
| `getVideos()` | `GET /videos` | 首页和影视列表共用 |
| `getVideo(id)` | `GET /videos/{video_slug}` | 视频详情与字幕分开请求 |
| 当前字幕数组 | `GET /videos/{video_id}/subtitles` | 后端按位置排序 |
| `getFavorites()` | `GET /me/favorites` | 登录后读取服务器数据 |
| `toggleFavorite()` | `PUT/DELETE /me/favorites/...` | 前端乐观更新，失败时回滚 |
| `setFavoriteStatus()` | `PATCH /me/favorites/{id}` | 状态值保持一致 |
| `getProgress()` | `GET /me/learning-summary` | 详情页进度随资源一并返回 |
| 本地简繁偏好 | `PATCH /me/preferences` | 游客仍可暂存本地，登录后同步 |

建议前端增加一个 API 访问层，页面组件不直接调用 `fetch`，也不直接依赖数据库字段。这样后端技术选型改变时，只修改访问层与 DTO 转换。

## 12. 并发、幂等与数据一致性

- 收藏使用 `PUT` 和数据库唯一约束保证幂等；
- 进度接口采用 upsert，不为每次播放生成新记录；
- 本地状态迁移要求 `Idempotency-Key` 或请求体 `idempotency_key`；
- 管理端更新内容建议提交 `updated_at` 或版本号，过期编辑返回 `409 CONFLICT`；
- 删除已被用户收藏的公开内容时，优先下架而非物理删除；
- 服务端忽略客户端提交的 `user_id`、`role`、派生进度百分比和审核状态；
- 批量字幕导入必须在事务中校验，失败时返回行号与原因，避免半批成功。

## 13. 缓存与性能

- 已发布课程、课节、视频和字幕允许 CDN 或服务端缓存；
- 用户资料、收藏、生词本和学习进度使用 `Cache-Control: private, no-store`；
- 视频文件直接由对象存储或 CDN 提供，支持 Range 请求；
- 字幕可按视频整体缓存，并用发布时间或内容版本作为缓存键；
- 公开响应中的个性化字段只在登录请求中动态合并；
- AI 和 TTS 任务使用后台队列，不占用普通 HTTP 请求直到完成。

## 14. 安全与权限边界

- 所有 `/me/*` 查询必须由会话确定用户，只能读写自己的数据；
- 普通用户不得读取草稿、AI 原始输出、审核备注或内容任务日志；
- 编辑者只能管理内容，只有管理员可修改用户角色；
- 字幕和课节内容写入前进行长度、类型和 HTML 清理；
- 上传接口校验文件类型、大小和实际媒体格式；
- 登录、注册、密码重置、AI 任务和上传接口分别限流；
- 日志对邮箱、令牌、API Key 和第三方原始敏感响应进行脱敏。

## 15. MVP 接口验收标准

接口进入实现阶段后，至少满足以下验收：

- 游客能读取已发布课程、课节、视频和字幕，不能写用户数据；
- 登录用户只能读写自己的收藏、生词本和学习进度；
- 字幕能按时间准确驱动播放器，单句可重播和收藏；
- 同一句字幕或课程内容重复收藏不会产生重复记录；
- 删除收藏不误删生词本，删除生词本不影响收藏；
- 视频和课节进度可跨设备恢复，旧请求不会覆盖更新进度；
- 简繁偏好登录后可同步，公开内容同时提供简繁字段；
- 未校对字段返回 `null`，不会冒充已审核内容；
- DeepSeek 失败不影响学习页面，AI 草稿不能直接自动发布；
- 普通接口和管理接口有明确权限隔离；
- 大型媒体文件不经过数据库或 Git 仓库传输；
- API 文档示例与实际响应可以通过自动化契约测试。

## 16. 本阶段暂不锁定的技术选型

以下选择可以在后端初始化时决定，不影响本接口契约：

- Node.js 或 Python 后端框架；
- 自建认证或托管认证服务；
- PostgreSQL 托管平台；
- 对象存储与 CDN 服务；
- 后台任务队列实现。

下一阶段应先建立数据库迁移、种子数据和接口骨架，再把当前前端的数据访问逐页替换为真实 API。
