# 粤见 CantoScene｜Demo 冲刺交接文档

> 文档日期：2026-08-10  
> 当前分支：`codex/mvp-frontend-baseline`  
> 当前基线提交：`a2d6d4d feat: connect frontend learning data`  
> 本文档是新任务的第一读取入口。它仅收缩 Demo 的范围与时间，不推翻《01—14》已确定的产品、设计、数据库和接口原则。

## 1. 本次目标

在 2—3 天内完成一版可打开、可演示、可走通核心流程的全栈 Demo。

Demo 的目标不是做成可大规模公开运营的正式产品，而是让体验者在几分钟内看懂产品定位，并完成一条真实学习路径：

> 进入首页 → 了解学习方式 → 学习“茶餐厅点餐”课程 → 听发音和对话 → 观看粤语视频 → 点击字幕看粤拼与释义 → 收藏台词／字词 → 在“我的学习”中看到记录 → 使用一次 DeepSeek 辅助解释。

## 2. Demo 完成定义

以下项目全部成立即视为 Demo 完成：

- 有一个可访问的前端页面，主要视觉不破版；
- 游客可浏览首页、课程和影视内容；
- 可注册、登录和退出，刷新后会话仍在；
- 至少一条“茶餐厅点餐”课程路径内容完整；
- 至少 5—10 个核心字词、3—5 句例句和一段情景对话可播放语音；
- 现有 `01.mp4` 可播放，字幕随时间显示，可展开台词信息并重播；
- 可收藏一句字幕、收藏一个字词，刷新后数据仍在；
- “收藏”和“我的学习”展示真实用户数据；
- 页面上有一个真实经过后端调用的 DeepSeek 能力；
- 项目已提交 Git，有基本 README、启动方式和演示说明；
- 前端、后端和标准 PostgreSQL 完成部署，可通过公开地址演示。

## 3. 优先级与范围

### P0：Demo 必须完成

1. “茶餐厅点餐”单条课程路径；
2. Fish Audio 静态语音生成、存放和播放；
3. 现有视频、字幕、单句重播和收藏；
4. 真实注册登录、收藏、生词本和视频进度；
5. 一个后端 DeepSeek 功能；
6. 一次完整演示路径验收；
7. GitHub 仓库说明和可展示的部署结果。

### P1：有余力再做

- 课程完成进度的更完整操作；
- 收藏后按原时间点回到视频；
- 收藏的“刚收藏／学习中／已掌握”状态切换；
- 课程和影视公开页面全部改为实时 API 数据；
- 手机端的明显布局问题；
- 简单的加载、错误和成功提示。

### 明确延后

- 录音、发音评分、语音识别和 AI 自由对话；
- 完整粤拼课程、真正的 100 字词和多门完整课程；
- 完整后台编辑器和用户内容投稿；
- 社区、排行榜、支付、订阅和运营活动；
- 大规模内容导入、全自动审核和生产级监控；
- 逐句精修 350 条自动转写字幕；
- 为了优化构建体积而进行大规模前端重构。

## 4. 当前项目真实状态

| 部分 | 当前状态 | Demo 冲刺中的处理 |
|---|---|---|
| 产品与设计文档 | 《01—14》已齐全 | 不重做设计，以现有规范为准 |
| 前端 | Vite + React + TypeScript，主要页面可打开 | 保留 Kimi 高保真方向，只修阻塞演示的问题 |
| 首页／课程／影视列表 | 多数仍使用原型 HTML 和前端示范数据 | 允许 Demo 保留；需确保主入口和真实内容能打开 |
| 视频学习 | `public/videos/01.mp4` 可播放，350 条字幕可跟随 | 保留当前视频，只精修 Demo 中会点到的少量字幕 |
| 字幕内容 | 自动转写，粤拼和普通话释义大量是占位 | 不声称已专业校对；精修少量演示台词 |
| 语音 | `AudioButton` 目前只有动画占位，没有真实音频 | P0：建立清单、生成 Fish Audio 文件并接入播放 |
| 后端 | Fastify + TypeScript + Prisma | 架构保留，不另起一套服务 |
| 本地数据库 | PGlite 持久化数据库，启动时自动迁移和种子化 | 本地 Demo 继续使用；部署改用标准 PostgreSQL |
| 用户系统 | 真实邮箱密码注册登录，access JWT + HttpOnly refresh cookie | 只做主路径冒烟验收，不扩展找回密码等功能 |
| 用户数据 | 收藏、生词本、课程／视频进度和学习概览接口已完成 | 验证一条端到端流程，修复阻塞问题 |
| DeepSeek | 尚未接入 | P0：做一个后端调用的可见学习功能 |
| Fish Audio | 已确定女声，但尚未生成课程文件 | P0：预生成静态音频，不在用户播放时调 TTS |
| Git | 当前工作树干净，有可回退阶段提交 | 每完成一个冲刺阶段就验收并提交 |

## 5. 已确定的产品与内容决策

- 中文名：粤见；英文名：CantoScene；
- 中文 Slogan：走进真实粤语。
- 英文 Slogan：See it. Hear it. Get Cantonese.
- 目标用户：粤语初学者，首批更偏向香港日常交流；
- 界面默认简体，可切换为香港繁体；
- 首批示范路径：茶餐厅点餐；
- 影视学习是 Demo 核心：播放视频、字幕同步、点击显示粤拼和普通话、重播和收藏；
- 首批视频由项目负责人提供，当前使用 `public/videos/01.mp4`；
- Demo 阶段不让版权审核阻塞开发，但不删除未来替换素材的能力；
- Demo 内容可使用待核验草稿，但不宣称为专业校对的权威答案。

### 茶餐厅课程的 Demo 内容边界

保留现有“5 课节”结构，不扩大为完整课程库。内容可围绕：

1. 进店与入座：“几位呀？”“一位，唔该。”
2. 说出想要的饮品：“想要一杯冻柠茶。”
3. 表达冷热和甜度：“冻”“热”“少甜”。
4. 加点食物和数量：“再要一个菠萝油。”
5. 加单与结账：“仲有冇其他嘢要呀？”“埋单，唔该。”

冲刺中可以只让其中一课具备最完整的字词、例句、对话和播放交互，其余四课保证标题、目标和核心内容可阅读即可。原始文案优先从《09-页面文案与示范内容》取用，不重新凭空创作一套。

## 6. 语音方案

### 已确定

- 服务：Fish Audio；
- 音色：项目负责人创建的统一女声；
- 音色 ID：`be60c03139d94dc7b0c26d83f66550db`；
- 音色页面：<https://fish.audio/zh-CN/app/m/be60c03139d94dc7b0c26d83f66550db/>；
- 使用方式：在内容生产阶段生成并下载静态音频，前端播放静态文件；
- 不在用户每次点击播放时调用 Fish Audio API。

### 最短实施路径

1. 先建立一份小型音频 manifest，每条包含稳定 ID、香港繁体台词、类型和输出文件名；
2. 只生成 Demo 真正会点击的 10—20 个文件；
3. 字词、例句和对话分目录保存，文件名不使用临时序号；
4. 把 `AudioButton` 从动画占位改为真实 HTML Audio 播放，同一时间只播一条；
5. 音频文件与内容 ID 对应，不把 base64 写入数据库。

Fish Audio 密钥如需使用，由项目负责人在本机 `.env` 中配置，不在对话中发送，不进入 Git，不进入前端构建产物。

## 7. DeepSeek Demo 方案

### 推荐功能：“AI 讲解这句”

在字幕学习卡上增加一个次要按钮。用户点击后，由后端把当前粤语台词和已有上下文发送给 DeepSeek，返回结构化内容：

- 这句话在当前场景中的意思；
- 1—3 个值得学的口语点；
- 语气或使用场景提示；
- 一句简短的类似表达。

### 必须保留的技术边界

- DeepSeek API Key 只存在后端环境变量；
- 前端只调用自己的后端接口，不能直连 DeepSeek；
- 设置超时、最大输入长度和简单限流；
- 要求 DeepSeek 返回严格 JSON，后端校验后再返回前端；
- 按字幕 ID + prompt 版本缓存结果，避免每次点击重复付费；
- 前端标注“AI 辅助解释·内容待核验”；
- DeepSeek 超时或失败时只显示可重试提示，不影响视频、字幕、收藏和已有释义；
- 不把 AI 回答直接覆盖已发布的字幕、粤拼或课程内容。

DeepSeek 密钥由项目负责人在开始接入时放入本地后端 `.env`，不应在对话中粘贴。

## 8. 2—3 天执行顺序

### 第一阶段：锁定演示内容和语音清单

1. 用 10—15 分钟完成真实注册、收藏、进度和刷新恢复冒烟测试；
2. 从《09-页面文案与示范内容》提取茶餐厅课程的最终小型内容集；
3. 把内容整理成结构化数据，保留稳定 ID；
4. 生成音频 manifest，开始生成 Fish Audio 静态文件；
5. 只精修会出现在演示流程中的少量文字。

### 第二阶段：接入真实内容、语音和 DeepSeek

1. 让一节茶餐厅课程完整使用确定内容；
2. 让字词、例句和情景对话真正播放 Fish Audio 音频；
3. 修复影视页演示时会点击的几句字幕；
4. 新增 DeepSeek 后端封装、结构验证、缓存和前端入口；
5. 跑通“课程 → 视频 → 收藏 → 我的学习”。

### 第三阶段：验收、部署和演示准备

1. 只修复阻塞主流程和明显影响观感的问题；
2. 用新账号完成一次端到端验收；
3. 执行类型检查、构建和后端集成测试；
4. 确定最简部署组合，生产数据库使用标准 PostgreSQL，不使用 PGlite；
5. 配置环境变量、运行迁移、导入 Demo 种子内容；
6. 整理 GitHub README、演示账号／演示步骤和已知限制；
7. 提交一个可回退的 Demo 版本。

## 9. 不可为赶进度破坏的底线

1. 不将 DeepSeek、Fish Audio、数据库或部署密钥写入前端、Git 或业务数据表；
2. 不绕开 Prisma migration 手工修改生产数据库结构；
3. 不把课程、字幕、音频之间的关系只靠页面临时序号硬编码；
4. 不把用户收藏和学习进度退回为全局共享的前端假数据；
5. 不让 DeepSeek 或 Fish Audio 失败阻止视频和已发布内容阅读；
6. 不用重写整个前端的方式解决局部页面问题；
7. 不删除原始设计文档和原型，不破坏当前可回退 Git 基线；
8. 未经校对的内容必须能被标记和后续替换，不写成不可编辑的 UI 文字。

## 10. 冲刺期验收策略

不追求所有浏览器、所有异常分支和所有页面的精细回归。每个阶段至少保留：

- `pnpm typecheck`；
- `pnpm build`；
- `pnpm --filter @canto-scene/server typecheck`；
- `pnpm --filter @canto-scene/server test`；
- 涉及数据库、鉴权或用户数据时，执行 `pnpm --filter @canto-scene/server test:integration`；
- 浏览器人工冒烟路径：首页 → 注册 → 课程 → 视频 → 收藏 → 刷新 → 我的学习 → 退出再登录。

已知的 Vite 大 chunk 警告不阻塞 Demo；只要生产构建成功且页面加载可接受，先不为此做大规模拆包。

## 11. 本地开发命令

在项目根目录 `/Users/mac/Documents/全栈项目搭建` 中使用两个终端：

```bash
# 终端 1：前端
pnpm dev

# 终端 2：后端 + 本地持久化数据库
pnpm dev:server:local
```

- 前端：<http://127.0.0.1:5173/>
- 后端：<http://127.0.0.1:3000/api/v1>
- 健康检查：<http://127.0.0.1:3000/api/v1/health>
- 本地数据库：`server/.data/pglite`（已忽略，不进 Git）

如果终端提示 `pnpm: command not found`，先使用 Codex 工作空间提供的 Node/pnpm 运行时，不要在项目内复制一份 Node。

## 12. 新任务接手顺序

新任务开始后：

1. 完整读取本文档；
2. 读取 `git status` 和最近 6 条 Git 提交，确认工作树状态；
3. 只按需读取《09-页面文案与示范内容》《10-前端实现说明》《13-数据库结构设计》和《14-前后端接口设计》的相关部分；
4. 不重建项目、不更换技术栈、不重做已完成的认证和数据库工作；
5. 先完成真实账号与学习数据冒烟验收，只修复阻塞问题；
6. 立即进入“茶餐厅内容锁定 + 音频 manifest”，不再横向扩展产品模块；
7. 每完成一个可验收阶段，更新本文档的当前状态并提交 Git。

### 新任务的第一个具体产出

不是继续写新规划，而是在完成快速冒烟验收后，直接产出：

1. 一份最终茶餐厅 Demo 内容数据；
2. 一份 Fish Audio 生成 manifest；
3. 一份只包含必要文件的音频生成／导入方案；
4. 接入首批真实音频并在课程页播放。

## 13. 需要项目负责人配合的时点

不要在冲刺开始时一次性要求所有素材。只在下列时点请求配合：

- Fish Audio 生成清单完成后：请项目负责人在 Fish Audio 中生成／下载，或在本机配置 API Key 后由脚本批量生成；
- DeepSeek 后端封装准备完成时：请项目负责人在后端 `.env` 中本地配置 API Key；
- 部署前：确定 GitHub 仓库和部署平台账号；
- 当真实演示素材不足时：再请求新的视频、封面或其他必要素材。

## 14. 已知限制，不要在新任务中误判

- 现有 350 条视频字幕主要用于播放与数据流转测试，不是已完成的语言教学内容；
- 粤拼和普通话释义存在占位文字，Demo 只修复主演示台词；
- 课程页的部分公开内容仍在前端原型中，不等于用户私有数据没有接后端；
- PGlite 只是本地开发方案，不是生产部署方案；
- `public/videos/01.mp4` 体积较大且已被 Git 忽略，部署时需要单独安排视频文件或对象存储；
- 注册登录、收藏、生词本和进度接口已经通过后端集成测试，新任务只需要做浏览器冒烟验收；
- 构建会报前端 chunk 较大的警告，当前不阻塞 Demo。

## 15. 当前可回退节点

- `bff36e9`：MVP 前端基线；
- `ab8713d`：API 与数据库脚手架；
- `2b5da01`：PGlite 本地数据库和种子工作流；
- `a45f217`：本地数据库首次启动修复；
- `c2a9ec0`：认证与学习数据 API；
- `a2d6d4d`：前端接入真实认证、收藏、生词本和进度。

新任务应从 `a2d6d4d` 继续，不回退或覆盖已完成工作。

## 16. 冲刺进展记录

### 2026-08-11｜第一阶段

- [x] 核对 Git 分支和最近可回退提交；
- [x] 通过隔离集成测试验证注册、鉴权、收藏、生词本、学习进度和用户隔离；
- [x] 锁定 5 课节茶餐厅 Demo 课程结构，将第 2 课设为完整示范课；
- [x] 新增结构化课程源数据 `content/demo/cha-chaan-teng.json`；
- [x] 新增 15 条 Fish Audio 机器可读 manifest 和人工生成清单；
- [x] 新增内容与音频引用自动校验 `pnpm content:check`；
- [x] 项目负责人已用既定 Fish Audio 女声生成前 3 条试听，文件已按 manifest 导入；
- [x] 已导入全部 15 条 MP3，并执行 `pnpm content:check:audio` 完整验收；
- [x] 完成第 2 课示范页：6 个核心字词、2 个句型、4 个例句、5 句对话、小练习与 15 条静态音频均已接入；

### 2026-08-14｜第二阶段

- [x] 新增 DeepSeek 后端代理与字幕卡“AI 讲解这句”入口；
- [x] 加入严格 JSON 校验、12 秒超时、输入长度限制、每 IP 简单限流和字幕 ID + prompt 版本内存缓存；
- [ ] 项目负责人在后端本地环境文件配置 `DEEPSEEK_API_KEY` 后完成真实 API 冒烟测试；
