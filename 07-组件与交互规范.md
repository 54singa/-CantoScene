# 粤见 CantoScene｜组件与交互规范

> 文档状态：第一版  
> 依赖文档：《06-视觉设计规范》  
> 适用范围：桌面网页版（移动端组件规范延后补充）；本文所有组件均已在高保真原型中出现  
> 原型位置：`design/mockup/`（home.html、video-study.html、lesson.html、courses.html、videos.html、favorites.html、my.html）  
> 共享样式：`design/mockup/css/site.css`

## 0. 组件总览

| 组件 | 原型类名 | 出现页面 |
|---|---|---|
| 全局导航 | `.nav` | 全部 |
| 主按钮／描边按钮／弱按钮 | `.btn-primary` `.btn-outline` `.btn-ghost` `.btn-weak` | 全部 |
| 标签 chip | `.chip-*` | 全部 |
| 学习状态印章 | `.stamp-state` `.stamp-*` | 收藏、我的学习 |
| 路线进度条 | `.route` | 课程、课节、我的学习 |
| 店铺卡 | `.shop` | 首页、基础课程 |
| 视频卡 | `.vcard` | 首页、影视列表 |
| 字词卡（点心纸） | `.dish` | 课节学习 |
| 餐牌导航 | `.menu` | 课节学习 |
| 瓷片发音格 | `.tile` | 基础课程、粤拼速查 |
| 胶片播放器 | `.theater` | 视频学习 |
| 字幕行／当前句／明信片卡 | `.row` `.row.current` `.postcard` | 视频学习 |
| 票根卡／生字卡／迷你票根 | `.ticket` `.wordcard` `.mini-ticket` | 收藏、我的学习 |
| 行程单卡 | `.itin` | 我的学习 |
| 统计印章环 | `.ring` | 我的学习 |
| 便利贴提示 | `.sticky` | 视频学习 |
| 播放小圆钮 | `.play-mini` | 全部 |

## 1. 全局导航

### 结构

左侧品牌文字标（粤见 CantoScene）→ 五个一级入口（首页／基础课程／影视学粤语／收藏／我的学习）→ 右侧简繁切换 + 登录／用户昵称。

### 状态

| 状态 | 表现 |
|---|---|
| 封面态（首页首屏） | 透明底，文字带白色光晕保证图上可读 |
| 纸面态（滚动后／内页） | 云白 86% 毛玻璃底 + 1px 底部分隔线 |
| 当前模块 | 港天蓝·深文字 + 2px 蓝色下划线 |
| 悬停 | 文字色加深为港天蓝·深，无位移 |

### 规则

- 一级入口数量和顺序不可变（产品约束 5）；
- 未登录显示「登录」按钮，登录后显示用户昵称，样式不变；
- 简繁切换是即时原地切换：不刷新页面、不改变滚动位置、不影响播放状态（产品约束 3、4）；
- 导航吸顶，高度 ≤ 60px。

## 2. 按钮体系

| 类型 | 用途 | 视觉 |
|---|---|---|
| 主按钮 | 每屏唯一主行动（开始学习、继续学习、登录） | 港天蓝实心胶囊，白字，浅蓝投影 |
| 描边按钮 | 次级行动（重播本句、上一步、标记完成） | 白底 + 1.5px 蓝边 + 蓝字 |
| 蜜桃按钮 | 收藏类行动 | 蜜桃粉浅底 + 蜜桃深字 |
| 弱文字 | 危险／低频操作（取消收藏、退出登录） | 灰文字，无框 |

### 状态

- 悬停：上浮 2px，投影加深一档（180ms）；
- 按下：回到原位，投影收回；
- 禁用：透明度 45%，不响应悬停；
- 加载：按钮文字替换为「…」呼吸点，宽度不变；
- 一屏最多一个主按钮。

## 3. 标签与印章

### 3.1 标签 chip

小圆角矩形（6px），浅底深字：蓝（链接／选中）、绿（入门／已完成）、黄（提示／示范）、灰（中性信息）。标签只读，不可点的标签不做悬停态；可作筛选的 chip（影视列表分类）有选中实心态。

### 3.2 印章 stamp

用于"学习状态"和"已收藏"这类带完成感的标记：2px 描边、8px 圆角、±1.5° 微旋转。颜色语义固定：

| 印章 | 颜色 |
|---|---|
| 刚收藏 | 港天蓝 |
| 学习中 | 奶杏黄·深 |
| 已掌握 | 成功绿 |
| 已收藏 | 蜜桃粉·深 |

印章不是按钮外观；当印章可点击切换状态时（收藏页），悬停显示下拉箭头，点击弹出三态选择（见 §9）。

## 4. 路线进度条

全站统一的进度语言：未完成段是虚线（像地图上的规划路线），已完成段是港天蓝实线；100% 完成时实线变为成功绿。

- 高度 6px，圆角端点；
- 旁注数字用等宽字体（55%、2/6）；
- 不出现百分比以外的花哨读数；
- 已学句数等小进度用同语言缩小版（`.mini-progress`）。

## 5. 卡片组件

### 5.1 店铺卡（课程单元）

条纹雨棚（单元一蓝白、单元二粉白、单元三黄白）+ 店号 + 店名小字 + 衬线单元名 + 一句话目标 + 课节数／时长 + 状态 chip + 进入链接。悬停上浮 4px。登录用户卡片带路线进度，游客不显示进度。

### 5.2 视频卡

16:9 封面（占位期：pastel 底 + 衬线大单字 + 中央播放圆钮）+ 难度角标（左上）+ 时长角标（右下，深色半透明底等宽白字）+ 片名 + 场景标签 + 进入链接。

- 观看中：封面底部叠加 5px 续播进度条，链接文案变为「继续观看」+ 等宽百分比；
- 与已学课程相关：标签行尾部追加黄色「相关单元」chip（带链接图标），不单独占行；
- 正式封面到位前，占位样式不可替换为第三方影视截图（产品约束 17）。

### 5.3 字词卡（点心纸卡）

课节核心字词使用：左侧点心纸编号（A1、A2…，蜜桃虚线框）+ 衬线大字词条 + 粤拼 + 释义，右侧词条播放钮 + 收藏；下方虚线分隔的例句行（粤语 + 粤拼 + 普通话 + 例句播放钮）。

交互：

- 词条播放钮播词条音频，例句播放钮播整句（内容规范 5.4，两段音频独立）；
- 播放中按钮变为暂停态并显示进行中状态（见 §8）；
- 收藏立即反馈（见 §10）。

### 5.4 票根卡（收藏·影视台词）

左侧打孔联：实心播放圆钮（重播原声片段）+ 竖排票根编号（No. 00:05，即原片时间码）；主区三层文字 + 来源 + 状态印章 + 「回到原视频」+ 弱化的「取消收藏」。卡片整体 ≤ 0.7° 微倾斜，顶部一角贴纸胶带。

### 5.5 生字卡（收藏·课程字词）

词条 + 粤拼 + 释义 + 词条播放钮，虚线分隔的例句行，底部来源课程 + 状态印章 + 「回到原课程」。

### 5.6 行程单卡（我的学习·继续学习）

左侧蓝白打孔条纹，内含最近学习项、路线进度、主按钮「继续学习」+ 备选链接。全页第一视觉优先级。

## 6. 视频学习页核心组件

布局遵守产品约束 9：左视频、右字幕（桌面端）。

### 6.1 胶片播放器

白卡 + 上下胶片齿孔边 + 16:9 画面区 + 底部控制条（播放钮、进度轨、当前时间／总时长、音量、全屏）。进度轨已完成段港天蓝，拖拽手柄白心蓝边圆点。

### 6.2 字幕行

每条字幕一行：等宽时间码 + 说话者标签 + 衬线粤语原文。面板左侧为胶片齿孔带。

| 状态 | 表现 |
|---|---|
| 未播到 | 正常墨色 |
| 已播过 | 透明度 55% |
| 当前播放句 | 奶杏黄荧光笔刷底纹（微有机圆角）+ 左侧三角标 +「正在播放」小字 |
| 主动展开句 | 原位展开为明信片卡（见 6.3） |

规则：

- 荧光笔（自动跟随）与明信片（主动学习）必须一眼可辨，二者可同时存在；
- 播放中字幕自动滚动保持当前句可见；用户手动滚动或点击某句时暂停自动跟随，继续播放后恢复；
- 点击任一字幕行：视频跳到该句开始时间 + 该行原位展开。

### 6.3 明信片展开卡

纸白卡 + 顶部纸胶带 + 时间码与说话者 + 粤语大字 + **邮戳框粤拼**（虚线描边、微旋转）+ 手写体普通话释义 + 操作行：「重播本句」描边钮 + 收藏钮。

规则：

- 同一时间只展开一张明信片；展开另一句时当前卡收起；
- 不使用弹窗、抽屉或跳转（产品约束 11）；
- 某句缺少解释时，邮戳框位置显示「解释待完善」，不生成虚构内容；
- 展开卡出现时字幕面板平滑撑开（200ms），不跳动。

### 6.4 单句重播

- 点击「重播本句」：视频从该句开始时间播放，到结束时间自动暂停；
- 重播期间按钮变为「正在重播」态（禁用其他句的重播钮）；
- 用户拖动进度条或点击其他句立即退出重播；
- 重播不自动收藏、不改变学习状态。

## 7. 粤拼与普通话展开规则

- 三层排版规范见《06-视觉设计规范》§5.2，全站一致；
- 字幕区默认只显示粤语原文，粤拼和普通话由用户主动展开（产品约束：逐层展示）；
- 课节、字词卡、收藏页默认完整显示三层（复习场景以读为主）；
- 简繁切换只改变字形，三层结构不变（产品约束 4）。

## 8. 音频播放按钮状态机

所有发音／原声按钮（`.play-mini`、播放器控制钮、词条钮）统一状态机：

```text
默认(▶) → 点击 → 加载中(呼吸点) → 播放中(⏸ + 按钮呼吸光圈)
播放中 → 点击/播完 → 默认
加载失败 → 错误态(红色描边 + 轻摇一次 + 提示文字) → 点击重试
```

- 同一时刻全站只允许一个音频在播：播放新音频时，上一个自动停止并复位；
- 播放中按钮保持可点（点击即暂停）；
- 音频失败不清空页面文字内容。

## 9. 学习状态选择

- 三态：刚收藏／学习中／已掌握，印章样式（§3.2）；
- 收藏页中印章可点：点击在印章下方弹出三态小菜单（纸白卡 + 1px 边 + 阴影），选中后印章换色换字，菜单收起，无页面跳转；
- 状态保存后印章旁短暂浮现「已保存」小字（1.5s 淡出）；
- 首版不做自动复习队列，状态仅作整理标记。

## 10. 收藏与取消收藏

- 点击收藏：按钮立即变为「已收藏」蜜桃印章 + 轻盖下动效（scale 1.06 → 1，180ms）+ 短提示「已保存到收藏夹」（不遮挡视频）；
- 再次点击取消：印章退回「收藏」描边态，提供 3 秒撤销小条；
- 未登录用户点击收藏：弹出登录提示框（§11），登录成功后回到当前视频与当前时间位置并完成收藏；
- 一句话只保留一个有效收藏状态。

## 11. 登录提示框

小对话框（非全屏遮罩页）：

- 纸白卡 + 细边 + 浅阴影，最大宽 360px，背后 40% 深色遮罩；
- 文案固定结构：一句收益（"登录后保存这句台词"）+ 一句补充 + 主按钮「登录 / 注册」+ 弱文字「暂时不用」；
- 关闭后完整保留当前页面状态（视频位置、展开句、滚动位置）；
- 禁止在用户首次打开页面时自动弹出。

## 12. 系统状态

### 12.1 加载

- 列表与卡片：骨架屏（云白·深色块呼吸），不显示空白页；
- 视频：播放器骨架 + 「正在加载」；字幕骨架行可延后于画面；
- 音频：按钮呼吸点（见 §8）。

### 12.2 空状态

统一结构：一句现状 + 一句引导 + 一个主按钮，配海鸥线条小图。示例：

- 收藏为空：「你还没有收藏台词／看视频时点一句字幕旁的收藏，原声和解释就会贴到这面墙上。／去体验第一个视频」；
- 我的学习无记录：「从第一节课开始」+「直接体验一个入门片段」双入口；
- 空状态不显示空图表、不出现 0 数据大字。

### 12.3 错误

| 场景 | 表现 |
|---|---|
| 视频加载失败 | 播放器原位提示 + 重试 + 返回列表，页面其他信息保留 |
| 字幕加载失败 | 视频可播，字幕区提示暂不可用 |
| 音频失败 | 见 §8 错误态 |
| 网络断开 | 顶部细条提示「网络好像断了」，不伪装成空内容 |
| 操作失败 | 说明原因 + 重试，不清空用户已输入内容 |

### 12.4 成功反馈

轻量即时：按钮状态切换 + 1.5s 小字提示；完成课节给出明确完成页／完成章 + 下一步推荐（下一课节或相关影视片段）。

## 13. 动效原则

| 场景 | 参数 |
|---|---|
| 悬停上浮 | translateY(-2～-4px)，150–180ms，ease-out |
| 卡片入场 | 透明度 + 上移 8px，220ms，错开 40ms |
| 盖章（收藏） | scale 1.06→1，180ms |
| 荧光笔切换 | 底纹淡入 160ms，不平移跳动 |
| 明信片展开 | 高度撑开 200ms + 内容淡入 |
| 页面锚点滚动 | 平滑滚动，≤ 400ms |

- 所有动效可被 `prefers-reduced-motion` 关闭为直接切换；
- 不使用循环播放的装饰动画（云、招牌保持静止）；
- 动效只反馈状态变化，不为炫技。

## 14. 键盘与无障碍

- 全站可 Tab 遍历：导航 → 主行动 → 内容卡 → 次级操作；
- 焦点态：2px 港天蓝外描边 + 2px 偏移，不删除默认焦点；
- 字幕行、明信片操作、播放钮均为真实按钮语义（`<button>`／`<a>`）；
- 视频学习页快捷键（桌面端）：空格播放／暂停、`←` `→` 上一句／下一句、`R` 重播当前句、`F` 收藏当前句；
- 正文对比度 ≥ 4.5:1，浅底标签使用对应深色文字；
- 所有插画与封面提供 alt 文本；装饰性图形 `aria-hidden`；
- 色彩不作为唯一状态提示（当前句同时有三角标 + 文字标签）。

## 15. 与前端实现的对应

- 组件类名与 `design/mockup/css/site.css` 一致，可作为 CSS 架构起点；
- 设计 token（色值、字体、圆角、阴影）以《06-视觉设计规范》§3–§6 为准；
- Mock 数据结构与接口约定在《10-前端实现说明》中定义；
- 移动端组件规范在启动移动端设计时补充，本文件暂不含。
