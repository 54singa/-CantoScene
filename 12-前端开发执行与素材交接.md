# 粤见 CantoScene｜前端开发执行与素材交接

> 阅读对象：负责正式前端开发的 Kimi 或其他模型  
> 当前状态：产品文档、视觉规范、桌面端高保真原型、组件规范、示范文案和前端实现说明已经完成  
> 当前目标：把静态 HTML 视觉原型实现为可交互、可验证的 React 前端 MVP

## 1. 开始前必须完整阅读

请按以下顺序完整阅读工作区文件，不要只依赖聊天摘要：

1. `README-前端设计交接.md`
2. `01-产品定义.md`
3. `02-MVP-PRD.md`
4. `03-课程与内容规范.md`
5. `04-信息架构与用户流程.md`
6. `05-低保真页面设计.md`
7. `06-视觉设计规范.md`
8. `07-组件与交互规范.md`
9. `08-响应式与高保真页面设计.md`
10. `09-页面文案与示范内容.md`
11. `10-前端实现说明.md`
12. `11-前端验收清单.md`
13. 本文档

同时查看：

- `design/mockup/`：静态高保真 HTML 原型；
- `design/screens/`：桌面端页面校验截图；
- `design/assets/`：当前可用的插画和图片。

## 2. 对现有原型的正确理解

`design/mockup/` 是视觉和布局原型，不是最终前端项目。

正式前端需要补齐：

- React 页面与组件；
- TypeScript 数据类型；
- 正式页面路由；
- 登录与游客状态；
- 简繁切换；
- 收藏与取消收藏；
- 学习进度；
- 视频与字幕同步；
- 点击字幕定位；
- 单句重播；
- 本地数据持久化；
- 加载、空状态和错误状态；
- 键盘操作和基础无障碍。

不得把现有静态 HTML 简单复制后宣称前端已经完成。

## 3. 当前开发范围

本期前端使用 Mock 数据和 localStorage 完成完整用户闭环，不等待真实后端。

需要实现：

1. 首页；
2. 基础课程首页；
3. 课节学习页；
4. 影视视频列表；
5. 视频学习页；
6. 收藏页；
7. 我的学习；
8. 登录／注册提示；
9. 粤拼发音速查；
10. 高频粤语字词；
11. 全局导航、简繁切换和必要状态页面。

本期不实现：

- 真实后端接口；
- 真实账号认证；
- 用户录音；
- 发音评分；
- AI 对话；
- 用户视频上传；
- 社区、排行榜和积分商城；
- 管理后台。

## 4. 技术基线

优先遵循 `10-前端实现说明.md`：

- Vite；
- React；
- TypeScript；
- React Router；
- CSS 自定义属性与模块化样式；
- React Context 和局部状态；
- opencc-js；
- lucide-react；
- 原生 HTML5 video；
- Mock 数据和 localStorage。

如果需要改变关键技术选型，必须先说明原因、影响和替代方案，得到用户确认后再改。不要为了个人偏好重写已经确认的方案。

## 5. 建议开发阶段

### 阶段一：项目骨架

完成：

- 创建正式前端项目；
- 配置 TypeScript、路由和基础样式；
- 建立设计 Token；
- 建立全局导航和页面容器；
- 导入当前设计资产；
- 建立 Mock 数据类型与数据访问层；
- 配置环境变量示例和 `.gitignore`；
- 保证项目能正常启动、构建和访问各路由。

阶段验收：

- 开发服务器可运行；
- 所有主要路由存在；
- 首页能够打开；
- 没有把密钥或本地环境文件写入仓库。

### 阶段二：首页与全局组件

完成：

- 游客首页；
- 全局导航；
- 简繁切换；
- 登录提示组件；
- 按钮、标签、卡片、印章、进度等基础组件；
- 桌面端视觉与高保真截图基本一致。

不要在这个阶段要求用户提供视频。

### 阶段三：视频学习页模拟版

这是全站最高优先级。

先不使用真实视频，完成：

- 播放器占位状态；
- 模拟播放时间；
- 字幕按照模拟时间高亮；
- 字幕列表自动跟随；
- 点击字幕进入主动学习状态；
- 点击字幕跳转到对应模拟时间；
- 原位展开粤拼和普通话；
- 单句重播状态机；
- 收藏与取消收藏；
- 未登录收藏后的登录返回；
- 观看进度保存；
- 从收藏页跳回对应视频时间；
- 视频、字幕和解释的加载／失败状态。

### 阶段三验收后：向用户索取第一段测试视频

不要等全部前端页面开发完成。

只有当以下条件全部满足后，才告诉用户“现在需要第一段测试视频”：

- [ ] 视频学习页视觉结构已经完成；
- [ ] 模拟播放时间能够运行；
- [ ] 当前字幕能够按时间切换；
- [ ] 点击字幕能够改变当前时间；
- [ ] 主动学习与自动跟随状态已经区分；
- [ ] 单句重播逻辑已经完成；
- [ ] 收藏台词能够保存开始时间和来源；
- [ ] 页面不存在阻塞测试的明显报错；
- [ ] 项目可以在用户本机正常启动。

满足条件后，应主动向用户发送：

> 视频学习页的模拟版已经完成，现在需要第一段测试视频来校准真实播放、字幕同步、单句重播和原声收藏。请提供一段 30 秒至 3 分钟的粤语 MP4 视频；优先 H.264 视频编码、AAC 音频、720p 或 1080p、对话声音清楚，最好包含 5～15 句香港日常粤语。没有字幕文件也可以，最好不要把字幕直接烧录在画面中。同时请用一句话说明人物和场景。

如果用户暂时没有合适视频，不得停止整个前端开发。继续使用模拟播放完成其他页面，并把真实视频验证标记为待办。

### 阶段四：接入第一段真实视频

用户提供视频后：

1. 先确认文件可以在浏览器播放；
2. 检查编码、画面比例、时长和音量；
3. 整理或创建第一版时间轴字幕；
4. 把视频放入约定资源目录；
5. 用真实 `currentTime` 替换该页面的模拟时间；
6. 校准每句字幕起止时间；
7. 验证点击字幕定位；
8. 验证单句播放能在正确结束位置暂停；
9. 验证收藏后可从收藏页回到原视频时间；
10. 保留无视频时的占位和错误状态。

接入视频时不要删除模拟数据能力；它仍用于自动测试、演示和其他尚无正式素材的视频卡片。

### 阶段五：课程与其他页面

完成：

- 基础课程首页；
- 课节学习页；
- 字词播放交互；
- 情景对话；
- 影视列表；
- 收藏页；
- 我的学习；
- 粤拼速查；
- 高频字词页；
- 登录态首页；
- 空、加载和错误状态。

课程音频暂时可以使用静音占位或测试文件，但播放按钮和状态机必须先完成。不要在课程文案尚未最终确认前批量消耗 Fish Audio 积分。

### 阶段六：整体验收

按照 `11-前端验收清单.md` 完整检查：

- 视觉还原；
- 用户主流程；
- 视频字幕专项；
- 简繁切换；
- 登录与游客；
- 收藏与复习；
- 课程与音频；
- 空状态和错误状态；
- 键盘与无障碍；
- 浏览器兼容和性能；
- 构建和启动说明。

发现问题应修复并重新验证，不要只给出未完成事项列表后结束。

## 6. 用户现在需要准备什么

目前不要求用户准备：

- 全部影视视频；
- 完整字幕；
- 30～50条完整高频词；
- 全部课程音频；
- Fish Audio API 密钥；
- 数据库；
- 后端接口；
- 真实登录账号；
- 单独 Logo 图片。

用户只需在阶段三完成后提供第一段测试视频。其他内容按开发进度逐批索取，不要一次要求用户准备全部素材。

## 7. Fish Audio 使用规则

- 已确定统一课程女声；
- 音色 ID：`be60c03139d94dc7b0c26d83f66550db`；
- 课程音频最终预先生成并作为静态文件播放；
- 前端运行时不直接调用 Fish Audio；
- 前端不需要 Fish Audio API 密钥；
- 不将任何密钥提交到 GitHub；
- 语音生成输入采用经确认的香港繁体粤语原稿；
- 在课程文案冻结前，不进行全量音频生成；
- 第一轮只生成少量字词、例句和对话用于检查读音与播放交互。

## 8. 桌面端与移动端

当前高保真设计以桌面端为主：

- 1280～1440px 为设计基准；
- 1024px 以上保证完整功能；
- 视频学习页桌面端为左视频、右字幕；
- 正式移动端高保真设计尚未补齐；
- 移动端产品约束仍然是上视频、下字幕。

本轮建议先完成桌面端 MVP，但代码结构和 CSS 不得故意阻碍后续响应式适配。移动端至少保证不严重溢出、内容可浏览；不要擅自宣称移动端已经正式验收。

如果用户希望本轮同时交付正式移动端，必须先补齐移动端高保真设计和验收范围，再进入移动实现。

## 9. 不可擅自改变的事项

1. 品牌名称“粤见 CantoScene”；
2. 中文 Slogan“走进真实粤语。”；
3. 英文 Slogan“See it. Hear it. Get Cantonese.”；
4. 默认简体、可切换繁体；
5. 香港粤语和香港日常交流定位；
6. 五个一级导航入口；
7. 视频学习是核心差异化；
8. 点击字幕后原位展开解释；
9. 收藏台词必须保留原视频和时间位置；
10. 游客先体验，保存行为发生时再登录；
11. MVP 排除录音、评分、AI对话和用户上传；
12. DeepSeek 不增加独立聊天入口；
13. Fish Audio 统一女声；
14. 不增加社区、排行榜、积分商城等范围外功能；
15. 不用未经用户同意的新视觉方向覆盖“粤语小城／云上港街”设计。

## 10. 工作方式要求

- 开始编辑前先检查现有文件和工作树；
- 保留用户已有修改，不覆盖无关内容；
- 使用正式前端目录，不破坏 `design/mockup/` 原型；
- 每完成一个阶段，运行构建和必要测试；
- 出现错误先定位并修复；
- 进度更新说明“已经完成什么、如何验证、下一步是什么”；
- 只有真正需要素材或用户选择时才暂停；
- 不把可自行完成的技术问题转交给用户；
- 不把密钥、会员凭据或个人信息写入代码和文档；
- 不未经允许推送仓库、部署服务器或发布网站。

## 11. 给 Kimi 的直接任务指令

用户可以直接发送以下内容：

> 请完整阅读工作区中的 `12-前端开发执行与素材交接.md`，并按其要求继续。先检查现有文档、设计原型和 Git 状态，然后从阶段一开始正式实现 React + TypeScript 前端。保留 `design/mockup/` 作为视觉参考，不要把静态 HTML 当作最终实现。使用 Mock 数据和 localStorage 完成前端闭环，不等待后端。优先完成视频学习页模拟版；只有达到文档中“阶段三验收后”的全部条件，才提醒我提供第一段测试视频。每完成一个阶段都运行构建和必要测试，汇报可验证结果后继续推进。未经我确认不要改变产品范围、品牌、视觉方向、技术基线，不要推送、部署或公开发布。

